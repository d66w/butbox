# Chrome 웹스토어 제출 자료

심사 화면에 그대로 붙여 넣을 수 있게 정리했습니다.
`[대괄호]`는 사람이 채워야 하는 값입니다. 채우지 않으면 `npm run check:release`가 실패합니다.

---

## 1. 기본 정보

| 항목 | 값 |
| --- | --- |
| 이름 | 붙박스 |
| 짧은 이름 | 붙박스 |
| 카테고리 | 업무 효율 (Productivity) |
| 언어 | 한국어 |
| 개인정보처리방침 URL | `https://[도메인]/privacy.html` |
| 홈페이지 URL | `https://[도메인]/` |
| 지원 이메일 | `[이메일]` |

---

## 2. 요약 설명 (132자 제한)

```
자주 쓰는 문구를 이름표 붙은 박스에 넣어두고, 검색해서 바로 복사하세요. 팀원과 실시간으로 함께 씁니다.
```

87자입니다.

---

## 3. 자세한 설명

```
같은 문구를 하루에 수십 번 다시 치고 있다면.

붙박스는 자주 쓰는 텍스트를 "이름표 붙은 고정 자리"에 넣어두는 도구입니다.
계좌번호, 환불 안내, 배송 문구처럼 매번 똑같이 보내는 것을 박스에 담아두고,
필요할 때 검색해서 한 번 눌러 복사합니다.

■ 이렇게 씁니다

1. 툴바 아이콘을 누르면 사이드 패널이 열립니다
2. 박스를 만들고 이름표를 붙입니다 (예: "환불 안내")
3. 빈 박스를 누르면 클립보드에 있는 내용이 바로 담깁니다
4. 다음부터는 검색해서 박스를 누르면 복사됩니다

■ 주요 기능

· 한국어 초성 검색 — "ㅎㅂ"만 쳐도 "환불 안내"가 나옵니다
· 띄어쓰기를 무시하는 검색 — "회사계좌"로 "회사 계좌"를 찾습니다
· 태그로 분류하고 #태그로 걸러 보기
· 즐겨찾기 — 팀원끼리 서로 독립적입니다
· 템플릿 변수 — {{고객명}} 처럼 적어두면 복사할 때 값을 물어봅니다.
  {{오늘}}, {{지금}}은 자동으로 채워집니다
· 키보드만으로 — 검색창에서 위아래로 고르고 Enter로 복사
· 우클릭 "붙박스에 저장" — 웹페이지에서 고른 텍스트를 바로 담습니다
· Ctrl+Shift+K 단축키
· 라이트 / 다크 모드

■ 팀으로 쓰기

스페이스를 만들어 초대하면 같은 박스를 함께 씁니다.
누가 내용을 바꾸면 실시간으로 반영되고, 내가 편집하던 중이라면
덮어쓰지 않고 "팀원이 수정함"을 알려줍니다.

■ 무료로 쓸 수 있는 범위

박스 10개, 개인 스페이스 1개, 팀 참여는 개수 제한 없음.
박스 하나에 10KB까지 담을 수 있습니다.

■ 개인정보

붙박스는 사용자가 방문하는 웹페이지를 읽지 않습니다.
클립보드는 빈 박스를 직접 누른 그 순간에만 읽습니다.
자세한 내용은 개인정보처리방침을 확인해 주세요.
```

---

## 4. 권한 사유 (심사에서 항목마다 요구함)

manifest에 선언한 권한과 1:1로 맞춰 두었습니다. `npm run check`가 이 대응을 검사합니다.

### `sidePanel`
확장의 화면 전체가 Chrome 사이드 패널입니다. 이 권한 없이는 UI를 띄울 수 없습니다.

### `storage`
로그인 세션과 마지막으로 보던 스페이스, 정렬 방식, 화면 모드(라이트/다크)를 사용자의 기기에 저장합니다. 사용자가 만든 문구 자체는 사용자 계정의 서버 스페이스에 저장되며, 이 권한은 로컬 설정에만 씁니다.

### `identity`
Google 계정으로 로그인하기 위해 `chrome.identity.launchWebAuthFlow`로 OAuth 창을 엽니다. PKCE 흐름을 사용하고, 확장은 사용자의 비밀번호를 보지 않습니다.

### `clipboardWrite`
이 확장의 핵심 동작입니다. 박스를 누르면 그 내용을 클립보드에 넣습니다.

### `clipboardRead`
**빈 박스를 사용자가 직접 눌렀을 때만** 클립보드를 한 번 읽어 그 박스에 담습니다. "복사해 둔 것을 저장할 자리에 넣는다"는 동작을 두 단계(붙여넣기 → 저장)에서 한 번의 클릭으로 줄이기 위한 것입니다.

읽지 않는 경우를 분명히 하면:
- 자동으로, 주기적으로, 백그라운드에서 읽지 않습니다
- 내용이 있는 박스를 누르면 읽지 않고 복사만 합니다
- 이미지·파일은 다루지 않고 텍스트만 처리합니다
- 읽은 내용은 사용자가 담기로 한 그 박스에만 저장되고 다른 곳으로 보내지 않습니다

관련 코드: `src/app.js`의 `saveClipboardToBox`, `src/clipboard.js`의 `readClipboardText`

### `contextMenus`
웹페이지에서 텍스트를 선택하고 우클릭했을 때 "붙박스에 저장" 항목을 넣습니다. 선택 영역이 있을 때만(`contexts: ["selection"]`) 나타납니다.

### `activeTab`
위 우클릭 저장에서, 저장한 문구와 함께 **출처를 같이 담을지 사용자에게 물어보기 위해** 현재 탭의 제목과 주소를 읽습니다. `activeTab`은 사용자가 확장을 직접 호출한 그 순간에만 열리므로, 상시 페이지 접근 권한이 아닙니다.

관련 코드: `background.js`의 `contextMenus.onClicked`

### `host_permissions: https://*.supabase.co/*`
백엔드가 Supabase입니다. 데이터 저장·조회(REST)와 실시간 동기화(WebSocket)를 위해 이 도메인과 통신합니다. 다른 도메인은 요구하지 않습니다.

### 요구하지 않는 권한 (참고)
`tabs`, `history`, `cookies`, `webNavigation`, `<all_urls>`, 그리고 모든 사이트 접근 권한을 **선언하지 않았습니다.** `npm run check`가 이 권한들이 다시 들어오면 실패합니다.

---

## 5. 데이터 사용 공개 (Privacy practices 탭)

체크할 항목:

| 데이터 유형 | 수집 | 용도 |
| --- | --- | --- |
| 개인 식별 정보 (이메일) | 예 | 계정 인증. Google OAuth로 받은 이메일을 계정 식별에만 씁니다 |
| 사용자 활동 | 예 | 제품 개선. 아래 설명 참고 |
| 웹사이트 콘텐츠 | 예 | 사용자가 직접 저장한 문구. 저장·동기화 목적 |

선언해야 하는 사실:
- 승인된 용도 외로 사용하거나 양도하지 않습니다
- 판매하지 않습니다
- 신용도 산정이나 대출에 쓰지 않습니다

**"사용자 활동"에 대한 보충 설명** (심사에서 자주 물어봄):

수집하는 것은 이벤트 이름과 아주 좁은 메타데이터뿐입니다. 박스 내용은 절대 보내지 않습니다.

- 이벤트 이름은 DB의 CHECK 제약으로 15개만 허용됩니다
- 속성 키는 `surface`, `lever`, `plan`, `count`, `role`, `mode`, `reason` 7개만 통과합니다
- 문자열 값은 32자를 넘으면 버립니다
- 서버 함수(`log_event`)가 클라이언트를 믿지 않고 한 번 더 걸러냅니다

관련 코드: `src/features/analytics.js`, `supabase/schema.sql`의 `log_event`
검증: `tests/analytics.test.js` — "box content can never reach analytics"

---

## 6. 스크린샷 (1280×800 또는 640×400, 최소 1장 최대 5장)

사람이 직접 찍어야 합니다. 아래 순서를 권합니다.

1. **박스 목록 (라이트)** — 박스 5~6개, 태그가 보이는 상태. 제품의 첫인상
2. **초성 검색** — 검색창에 `ㅎㅂ`, 결과로 "환불 안내"가 걸러진 화면. 차별점
3. **템플릿 변수 입력** — `{{고객명}}` 값을 묻는 모달
4. **박스 목록 (다크)** — 같은 화면의 다크 모드
5. **팀 스페이스** — 멤버 목록과 실시간 표시

찍을 때 주의:
- 실제 개인정보·실제 계좌번호를 넣지 마세요. 예시 값을 쓰세요
- 사이드 패널을 조금 넓게 열어 카드가 잘리지 않게 하세요
- 로그인 이메일이 나오는 하단 바가 찍히면 가려 주세요

---

## 7. 제출 전 확인

```bash
npm run check:release
```

이게 통과해야 제출할 수 있습니다. 자리표시자가 남아 있으면 실패합니다.

그리고 순서를 지켜야 하는 것:

1. 웹스토어에 **비공개(초안)로 먼저 업로드**합니다
2. 스토어가 발급한 공개키를 받아 `manifest.json`의 `key`를 그 값으로 교체합니다
3. 바뀐 확장 ID로 Supabase Redirect URL을 **다시 등록**합니다
4. 그 다음에 심사를 제출합니다

2번을 건너뛰면 개발 환경과 스토어 배포판의 확장 ID가 달라져 로그인이 깨집니다. 자세한 내용은 `README.md` §3.

---

## 8. 심사에서 반려될 수 있는 지점 (미리 대비)

| 지점 | 대비 |
| --- | --- |
| `clipboardRead` 사유 부족 | §4의 문구를 그대로 제출. "빈 박스를 직접 누른 순간에만"이 핵심 |
| 개인정보처리방침 URL 접속 불가 | 도메인 배포 후 `https://[도메인]/privacy.html`이 실제로 열리는지 확인 |
| 운영자 정보 미기재 | `PRIVACY.md`·`privacy.html`의 `[운영자]` `[이메일]` `[사업자 정보]` 채우기 |
| 스크린샷이 기능을 설명하지 못함 | §6의 2번(초성 검색)을 반드시 포함 |
| 원격 코드 실행 의심 | 해당 없음. 외부 스크립트를 불러오지 않고 CSP가 `script-src 'self'`입니다 |
